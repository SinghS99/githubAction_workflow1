name: first workflow
on: workflow_dispatch
jobs:
  first-job:
   runs-on: windows-latest
   steps:
     - name: print greeting
       run: |
           echo "Hello world"
           echo "Hello multiline workflow"
     - name: print goodbye
       run: echo "hello Bye"
